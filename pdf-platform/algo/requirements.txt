# algo — PDF Processing Core Engine
# =================================

# --- PDF 解析 ---
PyMuPDF>=1.23.0

# --- 图片处理 ---
Pillow>=10.0.0

# --- 数据模型 ---
pydantic>=2.0.0

# --- 转换器 ---
# DOCX
pdf2docx>=0.5.0
python-docx>=1.0.0

# PPTX
python-pptx>=0.6.21

# XLSX
openpyxl>=3.1.0

# HTML (标准库即可, 无需额外依赖)
# JPG
pdf2image>=1.16.0

# --- OCR ---
# PaddleOCR (PP-OCRv4, stable)
paddleocr>=2.9.0,<3.0
paddlepaddle>=2.6.0,<3.0
#
# Tesseract (fallback)
# pytesseract>=0.3.10
# tesseract-ocr system package required

# --- 重建 ---
# 依赖 PyMuPDF + Pillow (已包含)
